{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the TapPad Windows backend status window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
