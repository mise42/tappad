{
  "name": "tappad-desktop-host",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "NO_STRIP=1 tauri build",
    "build:appimage": "NO_STRIP=1 tauri build -- --bundles appimage",
    "build:locked": "NO_STRIP=1 tauri build -- --locked"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
