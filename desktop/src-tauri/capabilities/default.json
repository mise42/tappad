{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the TapPad Linux and Windows desktop host surface.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
