{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the TapPad Linux host surface.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
